var val=prompt("Enter your age");
if (val>21) {
    console.log("can drink");//inside the console
    document.write("can drink");
} else {
    console.log("cannot drink");//inside the console
    document.write("cannot drink");
}
